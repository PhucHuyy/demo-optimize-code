<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImportCsvProject"
        tools:targetApi="31">
        <activity
            android:name=".DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".database.SqliteDemoActivity" />
        <activity android:name=".memory.MemoryDemoActivity" />
        <activity android:name=".network.KotlinFeaturesActivity" />
        <activity android:name=".database.RecursionActivity" />
        <activity android:name=".memory.BitmapActivity" />
        <activity android:name=".memory.BitmapDownsamplingActivity" />
        <activity android:name=".network.CacheActivity" />
        <activity android:name=".Broadcast.BroadcastDemo1Activity" />
        <activity android:name=".Broadcast.BroadcastDemo2Activity" />
        <activity android:name=".database.QueryOptimizationActivity" />
    </application>

</manifest>